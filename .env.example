# Copy this file to `.env` before running `docker compose up`.
# These values are used by both the Postgres container and the comparison script.

POSTGRES_USER=demo
POSTGRES_PASSWORD=demo
POSTGRES_DB=taxi
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
